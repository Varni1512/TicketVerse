spring:
  application:
    name: ticketverse
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 15MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:varni1505@gmail.com}
    password: ${MAIL_PASSWORD:cqsvstlbkmopkosj}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/ticketverse}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate # Use flyway for schema migrations
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  port: ${PORT:8083}

logging:
  level:
    com.ticketverse: DEBUG
    org.springframework.security: DEBUG

app:
  jwt-secret: c2VjcmV0LWtleS1mb3ItdGlja2V0dmVyc2UtYXBwbGljYXRpb24tc2hvdWxkLWJlLWxvbmctZW5vdWdo
  jwt-expiration-milliseconds: 604800000

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:dtavy8ear}
  api-key: ${CLOUDINARY_API_KEY:123419319438223}
  api-secret: ${CLOUDINARY_API_SECRET:TEz8svDrcvXFL9vyObJRUaonmWk}
